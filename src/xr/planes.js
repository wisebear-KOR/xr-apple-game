// Plane detection wrapper — P1.
// TODO: iterate frame.detectedPlanes, visualize outlines, expose as apple spawn surfaces.
export function createPlaneTracker(/* scene */) {
  return { update(/* frame, refSpace */) {} };
}
